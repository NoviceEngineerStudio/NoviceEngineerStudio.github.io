---
import RootLayout from "./RootLayout.astro";

type NavLayoutProps = {
    title?: string;
};

const { title = '' } = Astro.props as NavLayoutProps;
---

<RootLayout title={title}>
    <slot/>
</RootLayout>